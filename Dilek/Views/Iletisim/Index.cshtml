@model Services.Results.ContactResult
@{
    ViewBag.Title = "İletişim - Dilek Fanila";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-lg-12">
    <hr>
    <h2 class="intro-text text-center">
        <strong>İLETİŞİM</strong>
    </h2>
    <hr>
</div>
<div class="col-md-8">
    <iframe width="100%" height="400" frameborder="0" scrolling="no" marginheight="0" marginwidth="0" src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d1258.758571418869!2d33.77511365516706!3d41.37735880591114!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x4084f0afbda3c30b%3A0x651ac88b7162f78!2sM%C3%BCnire+Medresesi!5e0!3m2!1str!2str!4v1494831328720"></iframe>
    @*<iframe width="100%" height="400" frameborder="0" scrolling="no" marginheight="0" marginwidth="0" src="https://maps.google.com/maps?q=Hepkebirler Mahallesi, Münire Medresesi, Feyzi Efendi Sokak, Kastamonu Merkez/Kastamonu, Turkey, &t=&z=17&ie=UTF8&iwloc=&output=embed"></iframe>*@

</div>
<div class="col-md-4">
    @foreach (var item in Model.Contents.Where(q=>q.DBCode==7))
    {
        <p>
            @item.Title:
            <strong>@item.Text</strong>
        </p>
    }
</div>
<div class="clearfix"></div>
<div class="row">
    <div class="col-lg-12">
        <hr>
        <h2 class="intro-text text-center">
            <strong>BİZE ULAŞIN</strong>
        </h2>
        <hr>
        @foreach (var item in Model.Contents.Where(q => q.DBCode == 11))
        {
            <p>
                @item.Text
            </p>
        }
        <form role="form" action="@Url.Action("SendUs", "Iletisim")" method="post">
            <div class="row">
                <div class="form-group col-lg-4">
                    <label>İsim Soyisim</label>
                    <input type="text" class="form-control" id="Name" name="Name">
                </div>
                <div class="form-group col-lg-4">
                    <label>eMail Adresi</label>
                    <input type="email" class="form-control" placeholder="kisi@abc.com" id="Mail" name="Mail">
                </div>
                <div class="form-group col-lg-4">
                    <label>Telefon Numarası</label>
                    <input type='tel' pattern="[0][0-9]{10}" class="form-control" id="Phone" name="Phone" placeholder="05123456789" maxlength="11">
                </div>
                <div class="clearfix"></div>
                <div class="form-group col-lg-12">
                    <label>Mesajınız</label>
                    <textarea class="form-control" rows="6" placeholder="Mesajınızı buraya yazabilirsiniz." id="Text" name="Text"></textarea>
                </div>
                <div class="form-group col-lg-12">
                    <input type="hidden" name="save" value="contact">
                    <button type="submit" class="btn btn-default">Gönder</button>
                </div>
            </div>
        </form>
    </div>
</div>